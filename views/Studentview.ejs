<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="csrf-token" content="<%= csrfToken %>" />
    <title>StudentView</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            margin: 0;
            padding: 0;
            background-image: url('./images/background-image.jpg');
            /* Replace with the actual image URL */
            background-size: cover;
            background-position: center;
            background-repeat: no-repeat;
            background-attachment: fixed;
        }

        .container {
            max-width: 800px;
            margin: 20px auto;
            background-color: rgba(255, 255, 255, 0.9);
            border-radius: 10px;
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
            overflow: hidden;
            padding: 20px;
            position: relative;
        }

        .header {
            background: linear-gradient(to right, #2a2a72, #009ffd);
            color: #fff;
            text-align: center;
            padding: 20px;
            border-radius: 10px 10px 0 0;
            position: relative;
            overflow: hidden;
        }

        .header img {
            max-width: 50px;
            position: absolute;
            left: 20px;
            top: 50%;
            transform: translateY(-50%);
        }

        h2 {
            margin: 0;
            font-size: 2em;
            font-weight: bold;
        }

        .student-details {
            background: rgba(255, 255, 255, 0.8);
            padding: 15px;
            border-radius: 10px;
            margin-bottom: 20px;
            text-align: center;
            display: none;
            /* Initially hidden */
        }

        .student-details h3 {
            margin: 0;
            font-size: 1.5em;
            color: #2a2a2a;
        }

        .student-details p {
            margin: 5px 0;
            font-size: 1em;
            color: #555;
        }

        .toggle-details {
            position: absolute;
            right: 20px;
            top: 50%;
            transform: translateY(-50%);
            cursor: pointer;
            color: #fff;
            font-size: 1.5em;
        }

        .outpass-card {
            border: none;
            /* Remove border */
            margin-bottom: 20px;
            padding: 15px;
            position: relative;
        }

        .outpass-card h5 {
            margin: 0;
            font-size: 1.2em;
            font-weight: bold;
        }

        .outpass-card p {
            margin: 5px 0;
            font-size: 0.9em;
            color: #555;
        }

        .status-badge {
            display: inline-block;
            border-radius: 5px;
            font-size: 0.9em;
        }

        .status-pending {
            background-color: #ffc107;
            color: #fff;
        }

        .status-accepted {
            background-color: #28a745;
            color: #fff;
        }

        .status-rejected {
            background-color: #dc3545;
            color: #fff;
        }

        .action-button {
            background-color: #2a2a72;
            color: #fff;

            border: none;
            border-radius: 5px;
            font-size: 0.9em;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        .action-button:hover {
            background-color: #1c1c4f;
        }
        .modal-overlay {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0, 0, 0, 0.7); /* Semi-transparent black background */
            z-index: 9999; /* Ensure the modal is on top of other content */
            overflow: hidden;
            backdrop-filter: blur(5px); /* Apply a blur effect to the underlying content */
        }
        
        /* Styles for the modal content */
        .modal-content {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            background-color: #fff;
            padding: 20px;
            z-index: 10000; /* Ensure the modal content is on top of the overlay */
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.5); /* Add a shadow for the modal content */
        }
        
        /* Styles for the close button */
        .close {
            position: absolute;
            top: 10px;
            right: 10px;
            cursor: pointer;
        }
    </style>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        function toggleStudentDetails() {
            const studentDetails = document.querySelector('.student-details');
            studentDetails.style.display = (studentDetails.style.display === 'none' || studentDetails.style.display === '') ? 'block' : 'none';
        }
        function viewQR(outpassId) {
            // Send an AJAX request to fetch the QR code BLOB data from the server
            fetch(`/getQRCode/${outpassId}`)
                .then(response => response.arrayBuffer())
                .then(data => {
                    // Convert the BLOB data to a base64-encoded URL
                    const qrCodeImageURL = `data:image/png;base64,${arrayBufferToBase64(data)}`;

                    // Display the QR code
                    const qrCodeImageElement = document.getElementById('qrCodeImage');
                    qrCodeImageElement.src = qrCodeImageURL;

                    // Show the modal overlay
                    const modalOverlay = document.getElementById('modalOverlay');
                    modalOverlay.style.display = 'block';

                    // Disable scrolling on the body while the modal is displayed
                    document.body.style.overflow = 'hidden';
                })
                .catch(error => {
                    console.error(error);
                });
        }

        // Function to convert an array buffer to a base64-encoded string
        function arrayBufferToBase64(buffer) {
            let binary = '';
            const bytes = new Uint8Array(buffer);
            for (let i = 0; i < bytes.byteLength; i++) {
                binary += String.fromCharCode(bytes[i]);
            }
            return btoa(binary);
        }


        function closeQR() {
            // Hide the modal overlay
            const modalOverlay = document.getElementById('modalOverlay');
            modalOverlay.style.display = 'none';

            // Clear the QR code image source
            const qrCodeImageElement = document.getElementById('qrCodeImage');
            qrCodeImageElement.src = '';

            // Re-enable scrolling on the body
            document.body.style.overflow = 'auto';
        }


    </script>
</head>

<body>
    <div class="container">
        <div class="header">
            <img src="./images/student-icon.png" alt="Student Icon">
            <h2>WELCOME</h2>
            <div class="toggle-details" onclick="toggleStudentDetails()">...</div>
        </div>
        <div class="student-details">
            <h3>Student Details</h3>
            <!-- Replace the following with actual student details -->
            <p>Name: John Doe</p>
            <p>Roll Number: 123456</p>
            <p>Department: Computer Science</p>
        </div>
        <div class="outpass-card">
            <div class="flex justify-center mb-1">
                <h5>REQUESTED OUTPASSES(TODAY)</h5>
            </div>
            <% for(let i=0; i < outpasses.length; i++) { %>
                <div class="border-2 rounded p-2">
                    <div class="mt-1 font-thin">
                        <h5 class="font-medium">Outpass <%= outpasses[i].id %>
                        </h5>
                        <% const OutpassDateTimeOut=new Date(outpasses[i].datetimeout); %>
                            <p>DateTimeOut: <%= OutpassDateTimeOut.toLocaleString('en-US', { timeZone: 'UTC' }) %>
                            </p>
                    </div>
                    <div class="mt-2 mb-1">
                        Status:
                        <span
                            class="font-thin p-1 status-badge <%= outpasses[i].status == false ? 'status-pending' : 'status-accepted' %>">
                            <%= outpasses[i].status==false ? "Pending" : "Accepted" %>
                        </span>
                    </div>
                    <% if (outpasses[i].status==true) { %>
                        <button class="mt-1 rounded font-thin action-button" onclick="viewQR('<%= outpasses[i].id %>')">
                            <span class="m-1 p-2">View QR</span>
                        </button>
                        <% } %>
                </div>
                <% } %>
        </div>
        <div id="modalOverlay" class="modal-overlay" style="display: none;">
            <div id="qrCodeContainer" class="modal-content">
                <span class="close" onclick="closeQR()">&times;</span>
                <img id="qrCodeImage" src="" alt="QR Code" />
            </div>
        </div>
        <div class="flex justify-center">
            <a href="/requestoutpass"
                class="inline-block mt-10  rounded border-2 border-green-600 px-6 pb-[6px] pt-2 text-xs bg-green-600 font-bold uppercase leading-normal text-white transition duration-150 ease-in-out hover:bg-green-500"
                data-te-ripple-init>
                MAKE REQUESTS
            </a>
        </div>
        <div class="mt-4 p-2">
            <div
                class="p-2 h-12 h-20 border-2 rounded-lg shadow-lg flex justify-center items-center text-xl font-medium leading-tight text-gray-800">
                <div
                    class="p-2 h-12 h-20  flex justify-center items-center text-xl font-medium leading-tight text-gray-800">
                    <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5"
                        stroke="currentColor" class="w-6 h-6 mt-1 mx-2 fill-red-500">
                        <path stroke-linecap="round" stroke-linejoin="round"
                            d="M14.857 17.082a23.848 23.848 0 005.454-1.31A8.967 8.967 0 0118 9.75v-.7V9A6 6 0 006 9v.75a8.967 8.967 0 01-2.312 6.022c1.733.64 3.56 1.085 5.455 1.31m5.714 0a24.255 24.255 0 01-5.714 0m5.714 0a3 3 0 11-5.714 0M3.124 7.5A8.969 8.969 0 015.292 3m13.416 0a8.969 8.969 0 012.168 4.5" />
                    </svg>
                    <h5>EMERGENCY OUTPASS</h5>
                </div>
            </div>
        </div>
        <div class="mt-4 p-2">
            <div
                class="p-2 h-12 h-20 border-2 rounded-lg shadow-lg flex justify-center items-center text-xl font-medium leading-tight text-gray-800">
                <div
                    class="p-2 h-12 h-20  flex justify-center items-center text-xl font-medium leading-tight text-gray-800">
                    <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5"
                        stroke="currentColor" class="w-6 h-6 mt-1 mx-2 fill-green-500">
                        <path stroke-linecap="round" stroke-linejoin="round"
                            d="M9 12.75L11.25 15 15 9.75M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
                    </svg>
                    <h5>ACCEPTED OUTPASSES</h5>
                </div>
            </div>
        </div>
        <div class="mt-4 p-2">
            <div
                class="p-2 h-12 h-20 border-2 rounded-lg shadow-lg flex justify-center items-center text-xl font-medium leading-tight text-gray-800">
                <div
                    class="p-2 h-12 h-20  flex justify-center items-center text-xl font-medium leading-tight text-gray-800">
                    <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5"
                        stroke="currentColor" class="w-6 h-6 mt-1 mx-2 fill-red-500">
                        <path stroke-linecap="round" stroke-linejoin="round"
                            d="M9.75 9.75l4.5 4.5m0-4.5l-4.5 4.5M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
                    </svg>
                    <h5>REJECTED OUTPASSES</h5>
                </div>
            </div>
        </div>
    </div>

</body>

</html